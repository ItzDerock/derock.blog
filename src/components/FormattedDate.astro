---
interface Props {
  date: Date;
  class?: string;
}
---

<time datetime={Astro.props.date.toISOString()} class={Astro.props.class}>
  {
    Astro.props.date.toLocaleDateString("en-us", {
      year: "numeric",
      month: "short",
      day: "numeric",
    })
  }
</time>
